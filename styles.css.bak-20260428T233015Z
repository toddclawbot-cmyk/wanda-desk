:root {
  --bg: #05060b;
  --bg-2: #0a0d17;
  --ink: #e9f7ff;
  --ink-dim: #7da7b8;
  --ink-mute: #4a6878;
  --line: #10283a;
  --line-soft: #0b1a27;
  --cyan: #00f0ff;
  --cyan-dim: #0ea3b1;
  --magenta: #ff2bd6;
  --magenta-dim: #b01c95;
  --lime: #b6ff3a;
  --amber: #ffb020;
  --red: #ff3b6b;
  --green: #3dff9a;
  --violet: #8b5cff;
  --shadow-cyan: 0 0 12px rgba(0, 240, 255, .55), 0 0 30px rgba(0, 240, 255, .18);
  --shadow-magenta: 0 0 10px rgba(255, 43, 214, .55), 0 0 28px rgba(255, 43, 214, .18);
  --mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  --display: "Space Grotesk", Inter, system-ui, sans-serif;
  --crt: "VT323", var(--mono);
}

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  background:
    radial-gradient(1200px 600px at 85% -10%, rgba(255,43,214,.10), transparent 60%),
    radial-gradient(900px 500px at 10% 110%, rgba(0,240,255,.10), transparent 60%),
    linear-gradient(180deg, #05060b 0%, #06080f 100%);
  color: var(--ink);
  font-family: var(--mono);
  min-height: 100vh;
  overflow-x: hidden;
  letter-spacing: .02em;
  -webkit-font-smoothing: antialiased;
}

/* ------- overlays ------- */
.scanlines {
  position: fixed; inset: 0; pointer-events: none; z-index: 9;
  background: repeating-linear-gradient(
    to bottom,
    rgba(255,255,255,.025) 0px,
    rgba(255,255,255,.025) 1px,
    transparent 2px, transparent 3px
  );
  mix-blend-mode: overlay;
}
.vignette {
  position: fixed; inset: 0; pointer-events: none; z-index: 8;
  background: radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,.65) 100%);
}
.grid-bg {
  position: fixed; inset: 0; pointer-events: none; z-index: 0;
  background-image:
    linear-gradient(rgba(0,240,255,.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,240,255,.06) 1px, transparent 1px);
  background-size: 48px 48px;
  mask-image: radial-gradient(ellipse at 50% 40%, #000 30%, transparent 75%);
  -webkit-mask-image: radial-gradient(ellipse at 50% 40%, #000 30%, transparent 75%);
}

/* ------- topbar ------- */
.topbar {
  position: sticky; top: 0; z-index: 10;
  display: flex; justify-content: space-between; align-items: center;
  padding: 14px 24px;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: linear-gradient(180deg, rgba(5,6,11,.85), rgba(5,6,11,.55));
  border-bottom: 1px solid var(--line);
}
.brand { display: flex; align-items: baseline; gap: 10px; }
.brand-mark {
  color: var(--cyan);
  text-shadow: var(--shadow-cyan);
  font-size: 22px; line-height: 1;
}
.brand-name {
  font-family: var(--display);
  font-weight: 800; font-size: 22px;
  letter-spacing: .25em;
  color: var(--ink);
  text-shadow: 0 0 1px var(--cyan);
}
.brand-sub {
  color: var(--magenta);
  font-weight: 700; letter-spacing: .3em; font-size: 12px;
  text-shadow: var(--shadow-magenta);
}
.topnav { display: flex; gap: 10px; flex-wrap: wrap; }
.chip {
  font-size: 11px; letter-spacing: .15em;
  padding: 6px 10px;
  border: 1px solid var(--line);
  border-radius: 3px;
  color: var(--ink-dim);
  background: rgba(10, 24, 36, .5);
}
.chip em { font-style: normal; color: var(--cyan); font-weight: 700; }
#breaker-chip em.armed { color: var(--lime); }
#breaker-chip em.tripped { color: var(--red); }
#recon-chip em.ok     { color: var(--lime); }
#recon-chip em.drift  { color: var(--red); animation: blink 1.2s step-end infinite; }
#recon-chip em.unknown { color: var(--ink-mute); }
@keyframes blink { 50% { opacity: .3; } }

/* ------- layout ------- */
.stage {
  position: relative; z-index: 1;
  display: grid; gap: 18px;
  padding: 22px;
  max-width: 1480px; margin: 0 auto;
  grid-template-columns: repeat(12, 1fr);
  grid-auto-rows: minmax(0, auto);
}
.hero            { grid-column: span 12; }
.chart-panel     { grid-column: span 8; min-height: 360px; }
.alloc-panel     { grid-column: span 4; }
.attr-panel      { grid-column: span 12; }
.positions-panel { grid-column: span 8; }
.trades-panel    { grid-column: span 4; }
.watch-panel     { grid-column: span 12; }

@media (max-width: 1100px) {
  .chart-panel, .alloc-panel, .positions-panel, .trades-panel { grid-column: span 12; }
}

/* ------- panel frame ------- */
.panel {
  position: relative;
  background:
    linear-gradient(180deg, rgba(10,16,26,.85), rgba(6,10,18,.85));
  border: 1px solid var(--line);
  border-radius: 6px;
  padding: 14px 16px 16px;
  overflow: hidden;
}
.panel::before {
  /* corner brackets */
  content: "";
  position: absolute; inset: 6px;
  pointer-events: none;
  background:
    linear-gradient(var(--cyan), var(--cyan)) top left / 14px 1px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) top left / 1px 14px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) top right / 14px 1px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) top right / 1px 14px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) bottom left / 14px 1px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) bottom left / 1px 14px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) bottom right / 14px 1px no-repeat,
    linear-gradient(var(--cyan), var(--cyan)) bottom right / 1px 14px no-repeat;
  opacity: .8;
  filter: drop-shadow(0 0 4px rgba(0,240,255,.6));
}
.panel-label {
  display: flex; justify-content: space-between; align-items: center;
  font-size: 10px; letter-spacing: .3em;
  color: var(--ink-dim);
  padding-bottom: 10px;
  margin-bottom: 10px;
  border-bottom: 1px dashed var(--line);
}
.panel-label .panel-meta { color: var(--ink-mute); letter-spacing: .12em; }

/* ------- hero ------- */
.hero-grid {
  display: grid;
  grid-template-columns: minmax(280px, 1.4fr) 2fr;
  gap: 22px;
  align-items: center;
}
@media (max-width: 760px) { .hero-grid { grid-template-columns: 1fr; } }
.hero-nav { padding: 8px 0; }
.nav-big {
  font-family: var(--display);
  font-weight: 800;
  font-size: clamp(44px, 7vw, 96px);
  line-height: 1;
  color: var(--cyan);
  text-shadow:
    0 0 2px rgba(0,240,255,.9),
    0 0 14px rgba(0,240,255,.55),
    0 0 40px rgba(0,240,255,.25);
  letter-spacing: -.01em;
}
.nav-delta {
  margin-top: 8px;
  font-size: 14px; letter-spacing: .2em;
  color: var(--ink-dim);
}
.nav-delta.up { color: var(--green); text-shadow: 0 0 8px rgba(61,255,154,.6); }
.nav-delta.down { color: var(--red); text-shadow: 0 0 8px rgba(255,59,107,.6); }
.hero-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}
@media (max-width: 600px) { .hero-stats { grid-template-columns: repeat(2, 1fr); } }
.stat {
  background: rgba(6,12,20,.6);
  border: 1px solid var(--line-soft);
  padding: 10px 12px;
  border-radius: 4px;
}
.stat-label { font-size: 10px; letter-spacing: .22em; color: var(--ink-mute); }
.stat-value { margin-top: 4px; font-size: 20px; font-weight: 700; color: var(--ink); font-family: var(--display); }
.stat-value.up { color: var(--green); }
.stat-value.down { color: var(--red); }

/* ------- chart ------- */
.chart-wrap { position: relative; height: 300px; }
.range-btn {
  font-family: var(--mono); font-size: 10px; letter-spacing: .2em;
  background: transparent; color: var(--ink-mute);
  border: 1px solid var(--line); border-radius: 3px;
  padding: 4px 8px; margin-left: 4px; cursor: pointer;
}
.range-btn:hover { color: var(--cyan); border-color: var(--cyan-dim); }
.range-btn.active { color: var(--cyan); border-color: var(--cyan); box-shadow: 0 0 6px rgba(0,240,255,.35) inset; }

/* ------- alloc ------- */
.alloc-grid { display: flex; flex-direction: column; gap: 10px; }
.alloc-row { display: grid; grid-template-columns: 80px 1fr 64px; gap: 10px; align-items: center; }
.alloc-label { font-size: 11px; letter-spacing: .2em; color: var(--ink-dim); }
.alloc-bar {
  position: relative;
  height: 10px;
  background: #071421;
  border: 1px solid var(--line-soft);
  border-radius: 2px; overflow: hidden;
}
.alloc-fill {
  position: absolute; inset: 0 auto 0 0;
  background: linear-gradient(90deg, var(--cyan), var(--magenta));
  box-shadow: 0 0 10px rgba(0,240,255,.45);
  transition: width .6s cubic-bezier(.2,.7,.2,1);
}
.alloc-cap {
  position: absolute; top: -2px; bottom: -2px; width: 2px;
  background: var(--amber);
  box-shadow: 0 0 6px rgba(255,176,32,.7);
}
.alloc-row.over .alloc-fill { background: linear-gradient(90deg, var(--red), var(--magenta)); }
.alloc-val { text-align: right; font-family: var(--display); font-weight: 700; font-size: 13px; color: var(--ink); }

/* ------- P&L attribution ------- */
.attr-summary {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
  margin-bottom: 14px;
}
@media (max-width: 760px) { .attr-summary { grid-template-columns: repeat(2, 1fr); } }
.attr-kpi {
  background: rgba(6,12,20,.6);
  border: 1px solid var(--line-soft);
  border-radius: 4px;
  padding: 10px 12px;
}
.attr-kpi .k-label { font-size: 10px; letter-spacing: .22em; color: var(--ink-mute); }
.attr-kpi .k-value {
  margin-top: 4px;
  font-family: var(--display);
  font-weight: 700;
  font-size: 20px;
  color: var(--ink);
}
.attr-kpi.pos .k-value { color: var(--green); text-shadow: 0 0 8px rgba(61,255,154,.35); }
.attr-kpi.neg .k-value { color: var(--red);   text-shadow: 0 0 8px rgba(255,59,107,.35); }
.attr-kpi .k-sub { font-size: 10px; color: var(--ink-mute); letter-spacing: .15em; margin-top: 2px; }

.attr-bars { display: flex; flex-direction: column; gap: 6px; }
.attr-row {
  display: grid;
  grid-template-columns: 88px 36px 1fr 1fr 110px;
  align-items: center;
  gap: 10px;
  padding: 6px 8px;
  border-radius: 3px;
  border: 1px solid transparent;
  transition: background .15s, border-color .15s;
}
.attr-row:hover {
  background: rgba(10, 20, 32, .5);
  border-color: var(--line-soft);
}
.attr-row .tk {
  font-family: var(--display); font-weight: 800; letter-spacing: .06em;
  color: var(--cyan); font-size: 14px;
}
.attr-row .cls {
  font-size: 9px; letter-spacing: .2em; color: var(--ink-mute); text-align: center;
  padding: 2px 4px; border: 1px solid var(--line-soft); border-radius: 2px;
}
.attr-neg-track, .attr-pos-track {
  position: relative;
  height: 18px;
  background: rgba(6,12,20,.5);
  border: 1px solid var(--line-soft);
  border-radius: 2px;
  overflow: hidden;
}
.attr-neg-track { justify-self: stretch; }
.attr-pos-track { justify-self: stretch; }
.attr-neg-fill, .attr-pos-fill {
  position: absolute; top: 0; bottom: 0;
  display: flex; align-items: stretch;
}
.attr-neg-fill { right: 0; flex-direction: row-reverse; }
.attr-pos-fill { left: 0; }
.attr-seg-unr { height: 100%; opacity: .85; }
.attr-seg-real { height: 100%; opacity: 1; }
.attr-pos-fill .attr-seg-unr  { background: linear-gradient(90deg, rgba(61,255,154,.25), rgba(61,255,154,.85)); box-shadow: 0 0 10px rgba(61,255,154,.3); }
.attr-pos-fill .attr-seg-real { background: linear-gradient(90deg, rgba(182,255,58,.25), rgba(182,255,58,.9));  box-shadow: 0 0 10px rgba(182,255,58,.3); }
.attr-neg-fill .attr-seg-unr  { background: linear-gradient(270deg, rgba(255,59,107,.25), rgba(255,59,107,.85)); box-shadow: 0 0 10px rgba(255,59,107,.3); }
.attr-neg-fill .attr-seg-real { background: linear-gradient(270deg, rgba(255,43,214,.25), rgba(255,43,214,.9));  box-shadow: 0 0 10px rgba(255,43,214,.3); }

.attr-zero {
  position: absolute; top: 0; bottom: 0;
  width: 1px; background: var(--cyan); opacity: .6;
  box-shadow: 0 0 4px rgba(0,240,255,.6);
  left: 50%;
}
.attr-zero-wrap { position: relative; grid-column: 3 / 5; }

/* hide mobile fallback on desktop */
.attr-mobile-bar { display: none; }

.attr-amt {
  font-family: var(--display); font-weight: 700; font-size: 14px;
  text-align: right; letter-spacing: .02em;
}
.attr-amt.pos { color: var(--green); }
.attr-amt.neg { color: var(--red); }
.attr-amt small {
  display: block; font-family: var(--mono); font-size: 10px;
  font-weight: 500; color: var(--ink-mute); letter-spacing: .14em;
}

.legend-dot {
  display: inline-block; width: 8px; height: 8px;
  margin: 0 4px 0 10px; border-radius: 1px;
  vertical-align: baseline;
}
.legend-unr  { background: var(--green); box-shadow: 0 0 6px rgba(61,255,154,.6); }
.legend-real { background: var(--lime);  box-shadow: 0 0 6px rgba(182,255,58,.5); }

/* ------- positions table ------- */
.table-wrap { overflow-x: auto; }
.pos-table { width: 100%; border-collapse: collapse; }
.pos-table th, .pos-table td {
  padding: 8px 10px; text-align: left; font-size: 12px;
  border-bottom: 1px solid var(--line-soft);
}
.pos-table th {
  font-size: 10px; letter-spacing: .22em; color: var(--ink-mute);
  background: rgba(6,10,18,.6);
  position: sticky; top: 0;
}
.pos-table td.num, .pos-table th.num { text-align: right; font-family: var(--display); font-weight: 600; }
.pos-table td.ticker { font-weight: 800; color: var(--cyan); letter-spacing: .08em; }
.pos-table td.class {
  font-size: 10px; letter-spacing: .2em; color: var(--ink-dim);
}
.cls-chip {
  display: inline-block; padding: 2px 6px;
  border: 1px solid var(--line); border-radius: 2px;
  font-size: 9px; letter-spacing: .22em;
}
.cls-equity { color: var(--cyan); border-color: var(--cyan-dim); }
.cls-crypto { color: var(--magenta); border-color: var(--magenta-dim); }
.cls-options { color: var(--lime); border-color: #72a020; }
.pnl.up { color: var(--green); }
.pnl.down { color: var(--red); }

/* ------- trades ticker ------- */
.trades-list {
  list-style: none; margin: 0; padding: 0;
  max-height: 360px; overflow-y: auto;
  display: flex; flex-direction: column; gap: 6px;
}
.trade {
  display: grid; grid-template-columns: 68px 1fr auto; gap: 8px;
  padding: 8px 10px;
  border: 1px solid var(--line-soft);
  border-radius: 3px;
  background: rgba(6,10,18,.55);
  font-size: 12px;
  align-items: center;
  min-width: 0;
}
.trade.reconcile {
  border-style: dashed;
  border-color: rgba(255,176,32,.3);
  background: rgba(255,176,32,.035);
}
.trade .act {
  font-size: 10px; letter-spacing: .18em; font-weight: 700; text-align: center;
  padding: 4px 2px; border-radius: 2px; border: 1px solid;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.act-BUY { color: var(--green); border-color: rgba(61,255,154,.4); background: rgba(61,255,154,.06); }
.act-SELL, .act-EXIT { color: var(--red); border-color: rgba(255,59,107,.4); background: rgba(255,59,107,.06); }
.act-TRIM { color: var(--amber); border-color: rgba(255,176,32,.4); background: rgba(255,176,32,.06); }
.act-RECONCILE_BUY, .act-RECONCILE_EXIT, .act-RECONCILE_RESTORE {
  color: var(--amber); border-color: rgba(255,176,32,.4);
  background: rgba(255,176,32,.06);
  letter-spacing: .12em;
  font-size: 9px;
}
.trade .body { min-width: 0; overflow: hidden; }
.trade .body .why {
  /* clamp long reconciliation messages to 2 lines with ellipsis */
  display: -webkit-box;
  -webkit-line-clamp: 2;
  line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  word-break: break-word;
}
.trade .when { white-space: nowrap; }
.trade .body { color: var(--ink); }
.trade .body .tk { color: var(--cyan); font-weight: 800; margin-right: 6px; }
.trade .body .why { color: var(--ink-mute); font-size: 11px; display: block; margin-top: 2px; }
.trade .when { color: var(--ink-mute); font-size: 10px; letter-spacing: .15em; }

/* ------- watchlist ------- */
.watch-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 10px;
}
.watch-card {
  border: 1px solid var(--line-soft);
  border-radius: 4px;
  padding: 10px 12px;
  background: rgba(6,10,18,.55);
  transition: transform .2s, border-color .2s, box-shadow .2s;
}
.watch-card:hover {
  transform: translateY(-2px);
  border-color: var(--cyan-dim);
  box-shadow: 0 0 12px rgba(0,240,255,.2);
}
.watch-card .tk {
  font-family: var(--display); font-weight: 800; font-size: 18px;
  color: var(--cyan); letter-spacing: .08em;
}
.watch-card .strategy {
  font-size: 10px; letter-spacing: .22em; color: var(--magenta); margin-top: 2px;
}
.watch-card .trigger {
  font-size: 11px; color: var(--ink-dim); margin-top: 6px; line-height: 1.45;
}
.watch-card .st-line {
  display: flex; justify-content: space-between; gap: 6px;
  font-size: 10px; letter-spacing: .15em; color: var(--ink-mute);
  margin-top: 8px; padding-top: 8px; border-top: 1px dashed var(--line-soft);
}
.watch-card .st-line b { color: var(--ink); font-weight: 600; }

/* ------- bottom bar ------- */
.bottombar {
  position: sticky; bottom: 0; z-index: 10;
  display: flex; justify-content: space-between; align-items: center;
  padding: 10px 24px;
  border-top: 1px solid var(--line);
  font-size: 11px; letter-spacing: .18em;
  color: var(--ink-mute);
  background: linear-gradient(0deg, rgba(5,6,11,.9), rgba(5,6,11,.5));
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}
.foot-left { display: flex; align-items: center; gap: 10px; }
.foot-right { display: flex; gap: 10px; flex-wrap: wrap; }
.foot-right em { font-style: normal; color: var(--ink-dim); }
.pulse-dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--lime);
  box-shadow: 0 0 8px var(--lime);
  animation: pulse 2s infinite;
}
.pulse-dot.stale { background: var(--amber); box-shadow: 0 0 8px var(--amber); }
.pulse-dot.dead  { background: var(--red);   box-shadow: 0 0 8px var(--red); animation: none; }
@keyframes pulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50%      { transform: scale(1.4); opacity: .6; }
}

/* ------- glitch ------- */
[data-glitch] {
  position: relative;
}
.glitch-on { animation: tear .35s steps(2) 1; }
@keyframes tear {
  0%   { transform: translate(0, 0); }
  20%  { transform: translate(-1px, 1px); }
  40%  { transform: translate(1px, -1px); }
  60%  { transform: translate(-2px, 0); }
  80%  { transform: translate(2px, 0); }
  100% { transform: translate(0, 0); }
}

/* ------- boot screen ------- */
.boot {
  position: fixed; inset: 0; z-index: 50;
  background: #05060b;
  display: grid; place-items: center;
  padding: 24px;
}
.boot pre {
  font-family: var(--crt);
  font-size: 18px;
  color: var(--cyan);
  text-shadow: 0 0 6px rgba(0,240,255,.55);
  line-height: 1.25;
  white-space: pre-wrap;
  max-width: 680px;
}
.boot.done { opacity: 0; pointer-events: none; transition: opacity .4s ease; }

/* ------- scrollbars ------- */
*::-webkit-scrollbar { width: 8px; height: 8px; }
*::-webkit-scrollbar-thumb { background: var(--line); border-radius: 4px; }
*::-webkit-scrollbar-thumb:hover { background: var(--cyan-dim); }

/* =========================================================
   MOBILE — everything below is phone / small-tablet overrides.
   Breakpoint order: 900 → 640 → 420.
   Touch-friendly sizing (44px targets), no horizontal scroll,
   single-column stacking, NAV readout stays legible.
   ========================================================= */

/* ---- tablets & small laptops ---- */
@media (max-width: 900px) {
  .stage { padding: 14px; gap: 12px; }
  .topbar { padding: 10px 14px; flex-wrap: wrap; gap: 8px; row-gap: 8px; }
  .bottombar { padding: 10px 14px; flex-wrap: wrap; gap: 8px; row-gap: 6px; }

  /* stack all mid-size panels */
  .chart-panel, .alloc-panel, .attr-panel, .positions-panel, .trades-panel {
    grid-column: span 12;
  }
  .chart-wrap { height: 260px; }
}

/* ---- phones ---- */
@media (max-width: 640px) {
  .stage { padding: 10px; gap: 10px; }
  .panel { padding: 12px 12px 14px; }
  .panel::before { inset: 4px; }              /* pull corner brackets closer */
  .panel-label { font-size: 9px; gap: 6px; flex-wrap: wrap; row-gap: 4px; }
  .panel-label .panel-meta { letter-spacing: .08em; font-size: 9px; }

  /* topbar: hide brand subtitle on phones, keep chips readable */
  .topbar { padding: 10px 12px; }
  .brand-name { font-size: 18px; letter-spacing: .18em; }
  .brand-sub  { display: none; }
  .chip { font-size: 10px; letter-spacing: .1em; padding: 5px 8px; }

  /* hero — mobile-first typography: big NAV number, stats below */
  .hero-grid { grid-template-columns: 1fr; gap: 14px; }
  .nav-big {
    font-size: clamp(44px, 13vw, 72px);
    line-height: .95;
    word-break: break-all;             /* never let the $ number clip */
  }
  .nav-delta { font-size: 12px; letter-spacing: .14em; }
  .hero-stats { grid-template-columns: repeat(2, 1fr); gap: 8px; }
  .stat { padding: 8px 10px; }
  .stat-label { font-size: 9px; letter-spacing: .16em; }
  .stat-value { font-size: 15px; }

  /* chart — shorter on phones, thicker line so it reads */
  .chart-wrap { height: 220px; }
  .range-btn { padding: 6px 10px; min-height: 30px; }  /* touch target */

  /* allocation rows — make the ticker column narrower */
  .alloc-row { grid-template-columns: 64px 1fr 52px; gap: 8px; }
  .alloc-label { font-size: 10px; letter-spacing: .12em; }
  .alloc-val { font-size: 12px; }

  /* P&L attribution — collapse to a single-bar layout on phones
     (negative / positive halves were unreadable at mobile widths) */
  .attr-summary { grid-template-columns: repeat(2, 1fr); gap: 6px; margin-bottom: 10px; }
  .attr-kpi { padding: 8px 10px; }
  .attr-kpi .k-label { font-size: 9px; letter-spacing: .14em; }
  .attr-kpi .k-value { font-size: 15px; }
  .attr-kpi .k-sub { font-size: 9px; letter-spacing: .08em; }
  .attr-row {
    grid-template-columns: 56px 1fr 96px;
    gap: 6px;
    padding: 6px 4px;
  }
  .attr-row .cls { display: none; }          /* free up space */
  .attr-row .tk  { font-size: 12px; }
  .attr-row .attr-neg-track,
  .attr-row > .attr-pos-track {
    display: none;                           /* left+right track grid doesn't work on phones */
  }
  .attr-row .attr-mobile-bar {
    display: block;                          /* show mobile single-bar */
    position: relative;
    height: 14px;
    background: rgba(6,12,20,.5);
    border: 1px solid var(--line-soft);
    border-radius: 2px;
    overflow: hidden;
  }
  .attr-row .attr-mobile-fill {
    position: absolute; top: 0; bottom: 0;
    border-radius: 2px;
  }
  .attr-row.pos .attr-mobile-fill { background: linear-gradient(90deg, rgba(61,255,154,.35), rgba(61,255,154,.9)); box-shadow: 0 0 8px rgba(61,255,154,.35); }
  .attr-row.neg .attr-mobile-fill { background: linear-gradient(90deg, rgba(255,59,107,.35), rgba(255,59,107,.9)); box-shadow: 0 0 8px rgba(255,59,107,.35); }
  .attr-amt { font-size: 12px; }
  .attr-amt small { font-size: 9px; letter-spacing: .06em; }

  /* positions table — horizontal scroll with sticky ticker column */
  .pos-table { font-size: 11px; }
  .pos-table th, .pos-table td { padding: 6px 8px; font-size: 11px; }
  .pos-table th { font-size: 9px; letter-spacing: .14em; }
  .pos-table td.ticker { position: sticky; left: 0; background: rgba(6,10,18,.95); z-index: 1; }
  .pos-table th:first-child { position: sticky; left: 0; z-index: 2; }
  .cls-chip { font-size: 8px; letter-spacing: .14em; padding: 1px 4px; }

  /* trades — compress the action chip column */
  .trade { grid-template-columns: 56px 1fr auto; gap: 6px; padding: 7px 8px; font-size: 11px; }
  .trade .act { font-size: 9px; letter-spacing: .1em; padding: 3px 2px; }
  .act-RECONCILE_BUY, .act-RECONCILE_EXIT, .act-RECONCILE_RESTORE { font-size: 8px; }
  .trade .body .tk { margin-right: 4px; }
  .trade .body .why { font-size: 10px; -webkit-line-clamp: 2; line-clamp: 2; }
  .trade .when { font-size: 9px; letter-spacing: .08em; }

  /* watchlist — single-column on narrow phones */
  .watch-grid { grid-template-columns: 1fr; gap: 8px; }
  .watch-card { padding: 9px 10px; }
  .watch-card .tk { font-size: 16px; }
  .watch-card .strategy { font-size: 9px; letter-spacing: .14em; }
  .watch-card .trigger { font-size: 10px; }
  .watch-card .st-line { font-size: 9px; letter-spacing: .08em; }
  /* disable lift-on-hover — can look stuck on touch devices */
  .watch-card:hover { transform: none; }

  /* footer tightens + wraps naturally */
  .foot-right { gap: 6px; font-size: 10px; }
  .foot-left  { font-size: 10px; }

  /* reduce overlay intensity slightly on smaller screens for readability */
  .grid-bg { background-size: 32px 32px; }
}

/* ---- small phones (iPhone SE, etc.) ---- */
@media (max-width: 420px) {
  .nav-big { font-size: clamp(38px, 14vw, 54px); }
  .hero-stats { grid-template-columns: repeat(2, 1fr); }
  .topnav { width: 100%; justify-content: flex-start; flex-wrap: wrap; }
  .attr-summary { grid-template-columns: repeat(2, 1fr); }
  .bottombar { font-size: 9px; }
  .foot-right span:nth-child(1),  /* hide inception on micro screens */
  .foot-right span:nth-child(2) { display: none; }
}

/* ---- reduced motion — respect user preference ---- */
@media (prefers-reduced-motion: reduce) {
  * { animation-duration: .01ms !important; transition-duration: .01ms !important; }
  .pulse-dot { animation: none; }
  .glitch-on { animation: none; }
  .scanlines { opacity: .4; }
}
